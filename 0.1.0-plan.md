# 0.1.0 — Project plan

Implements `0.1.0-design.md`. Self-contained: a fork can execute this plan with the design
document alone. CPU JS + canvas only; every phase ends with a page that draws something new
and with its `experiments/` checks printing PASS.

## 0. Conventions

- Runtime per `AGENTS.md`: classic `<script>` tags, file:// friendly, no build, tabs,
  no allocation in the frame loop, `module.exports` guarded so every file runs under node.
- One unit system (design §9): m, Myr, m/Myr, 1/Myr; the eruptive clock and the slider
  ranges are the only seconds/yr quantities and are converted at the boundary. Never put a
  per-year rate into `params.js` next to a Myr rate.
- Headless rule: `geom`, `state`, `sim.step` and every kernel are DOM-free, and the body
  raster writes into a caller-supplied buffer (`render.body(st, px, w, h)`); only
  `render.present()` and `ui.js` touch the DOM. That is what makes `raster-bench.js` time
  the real path under node.
- Load order lives once, in `index.html`; `experiments/lib.js` parses the `<script src>`
  tags and requires exactly that order, so node and the browser cannot drift.
- The camera moves only through `GEO` (`lookAt`/`panBy`/`zoomAt`/`setPreset`), which marks
  the view dirty; `GEO.rebuild` clears it. Assigning `P.view` directly cannot happen.
- One file per entity or concern; each sim kernel is a function `(state, dt)` reading the
  previous buffers and writing its own output. Mark + gather for all cross-entity transfer.
- All state in typed arrays with fixed capacity (`js/state.js` owns allocation);
  `js/params.js` owns the defaults of design §9. Float64Array for geometry and fields,
  integer arrays for ids and enums.
- Hot path rules from `AGENTS.md`: preallocated scratch, reused LUTs rebuilt only on view
  change, no closures/strings inside a frame, HUD text at 2 Hz.
- Validation harness = `experiments/*.js` node scripts printing PASS/FAIL and the measured
  numbers (this repo has no test runner). `experiments/smoke.js` loads the page scripts in
  page order under a minimal DOM shim and runs frames in node — every phase's wiring must
  pass it. A phase is done when its scripts pass and the result is recorded in the worklog.
- Worklog: `archive/0.1.0-worklog.md` starts at M0 and gets one entry per completed phase
  (steps, validation, numbers), always ending with a next-step suggestion. The finished plan
  moves to `archive/` at M6.3 (per the `AGENTS.md` workflow).
- Scales, maps and any number quoted from the design are reproducible with
  `node experiments/scale-check.js`; extend that script rather than re-deriving numbers.

## 1. Repository layout (target)

```
index.html                   page: canvas, two time sliders, HUD; scripts in order
js/params.js                 constants (design §9) + seed + slider state
js/rng.js                    seeded xorshift128
js/geom.js                   row schedule, fan topology, display maps + LUT buffers
js/state.js                  typed arrays (columns, stacks, plates, ribbons, plumes,
                             vents, deposits, fan T), reset, widths/mass/hash
js/surface.js                isostatic elevation + column profile (static, M1)
js/columns.js                layer stack ops (push/merge/sums/removeTop/layerAt), the
                             initial planet, fan-T lid (M1); later move/wrap/spawn/consume
js/mantle.js                 Tm(t), streamfunction flow, plume rise, fan T step, ext
js/slab.js                   ribbons: descent, dehydration, wedge melt supply, dissolve
js/plates.js                 plate solve (mean fit, relax, vMax), boundary classify
js/columns.js                column move/wrap, layer stack ops, spawn/consume/compaction
js/crust.js                  rift shares, accretion, collision, arc growth, damage,
                             collapse diffusion, zDyn + flexure, ore accumulation
js/surface.js                isostasy profile, erosion, one-hop routing, deposition
js/magma.js                  chamber fill/spill, eruption schedules (eruptive clock)
js/erupt.js                  toy box: pile CA, particles, write-back, edifice records
js/ore.js                    potentials helpers, deposit records, extraction
js/render.js                 LUTs apply, body raster (ImageData), overlay (canvas 2D)
js/ui.js                     sliders, pan/zoom + presets, probe, HUD, save/load buttons
js/sim.js                    frame orchestration: K0-K9 order, event cadence, diag
js/perf.js                   laps, smoothed fps/step, HUD text at 2 Hz
experiments/lib.js           node loader: requires js/* in index.html order, PASS/FAIL helper
experiments/scale-check.js   scale tables + assertions (cited by the design)
experiments/view-check.js    row schedule, fan stencil, display maps, camera, column LUT
experiments/smoke.js         DOM-stub page (parsed from index.html) + frames + determinism
experiments/raster-bench.js  stack ops + bed persistence + body raster <= 6 ms at 4 presets
experiments/ledger-check.js  mass/ore ledger + finite guards over a synthetic run
experiments/erupt-bench.js   toy CA + particles steps per second
experiments/logs/            saved outputs cited by the worklog
archive/                     implemented plans and worklogs
archive/reference/           vendored planet-geotectonics 0.1.5 design (physics source)
```

## 2. Phases

### M0 — Scaffolding and scales (1–2 d)

M0.1 `params.js`, `rng.js`, `geom.js`: row schedule (`y_i`, `h_i` closed forms), fan
    topology tables (`band`, `pitch`, neighbour lists across coarse-fine faces), display
    maps `u(y)`, `y(u)` and the two LUT builders.
M0.2 `state.js`: the full fixed-capacity layout of design §2 (columns, stacks, plates,
    ribbons, plumes, vents, toy boxes, deposits, fan T, ledger) + reset — the design pins
    every field, so the M0 layout is the final layout, no rework in later phases.
    `sim.js` loop with both clocks (K0 clocks/Tm and queues cadence events; topology changes
    after K4, K1–K9 slots empty),
    `perf.js`, `ui.js`: two sliders (log feel), pan/zoom (per-axis presets: default,
    overview, crust, basin), HUD.
M0.3 `render.js` debug view (toggleable): fan cells, row edges, real column ticks, plate
    boundary lines, the asinh grid lines, scale readout (km/px, m/px under cursor) — the
    section is a visible measuring tool from day one.
M0.4 `experiments/lib.js` + the view/scale/smoke harnesses; outputs saved to
    `experiments/logs/`.

Done: `node experiments/scale-check.js` and `view-check.js` print ALL PASS — `q` closed form
vs Σh = R, sky rows = 34, fan = 7155 with a 1-cell bottom band, the neighbour stencil
symmetric in face *and* distance with face conservation, `|y(u(y))−y| < 1e-12`, the column
LUT equal to a brute-force owner over wrap-crossing / x40 / non-uniform / 1-column /
0-column windows; the page draws the mesh; both sliders move their clocks.

### M1 — Stacks and the section look (2–3 d)

M1.1 `columns.js` layer stacks (push/merge-on-full/sums, removeTop, layerAt) over the M0
    buffers; `surface.js` static isostasy (elevation + wrapped slope) — moved forward from
    M2.3 because the initial relief *is* isostatic and the sediments need the basin shape.
    Initial planet: fbm land mask on a circle (seamless at the wrap) → quantile continental
    fraction → felsic thickness, oceanic `hMaf = hMafNew(Tm)`, seeded sediments in basins,
    age ramps from the initial ridges, half-space-cooling fan-T lid; profile → fill →
    profile.
M1.2 `render.js` headless body pass: per-screen-column profile (interpolated tops + relief
    noise scaled by local relief), then three pointer walks — air/water, the stack painter
    with bedding stripes down to the Moho, the fan below via the per-row LUT; prebuilt
    lith/mantle/water/air palette tables so the pass is multiply-add; overlay draws the
    profile and Moho lines. `ui.js` geology probe (column, layers, flags, age, mantle dT).

Done: a believable geological section at the four presets (bedding visible at x10/x40); the
50 m bed is a thin dark line at x10 (design §1.4) and stays exactly 50 m for 1e5 frames;
`experiments/raster-bench.js` ALL PASS with the body raster <= 6 ms at all four presets;
the probe reads lith/age/thickness/flags of any picked bed; `smoke.js` and `view-check.js`
stay green.

Done: a believable geological section at the three presets; a 50 m bed is a thin dark
line at x10 zoom (design §1.4 table); probe reads lith/age/thickness of any picked bed;
`experiments/raster-bench.js` within budget (≤ 6 ms).

### M2 — Tectonics (4–5 d)

Implement the base mantle driver, plate kinematics, K3 movement/classification, K4 contact
resolution and the first dynamic surface response. Plumes and ribbon descent remain M4;
erosion and sediment routing remain M3. Keep every kernel deterministic, headless and
allocation-free in the frame loop. `dtGeo = 0` must leave all tectonic state and mass
unchanged; a fixed seed and identical frame inputs must reproduce bitwise-identical state.
All material changes are explicit transfers, production or sinks in the per-lithology mass
ledger.

**M2.0 — state and test gates.** Audit and reuse `state.js`: `colPlate`, `colW`, `edge`,
`edgeAge`, `damage`, `zDyn`, `plU`, `plUP`, `plDmg`, `ledProd`, `ledCons`, `ledMix` and
`massBy` already exist. Do not duplicate them or add a plate phase without a defined use.
Add only missing fixed-capacity data: column velocity and extension (`colU`, `ext`),
boundary relative normal speed and polarity (`edgeRelN`, `edgePol`), the right-neighbour
plate id needed to validate edge history (`edgeRPlate`), per-column `trenchDist` (0 off-arc,
1–3 on the overriding side), and preallocated sort/old-width scratch. Define `edge[i]` as
the boundary from sorted column `i` to its right neighbour across the wrap; `edgePol[i]` is
−1 when the left column subducts, +1 when the right column subducts, and 0 otherwise.
Carry hysteresis only while that ordered plate pair is unchanged. Add per-lithology
`ledMixIn`/`ledMixOut` volumes alongside the `ledMix` event count, so cross-lith stack
compaction is explicit. `experiments/m2-check.js` checks finite values at each kernel boundary and, after K4,
sorted unique wrapped positions, widths summing to `P.wrap`, plate membership/counts, layer
sums, and, for each lithology,
`column mass + ledCons + ledMixOut = initial mass + ledProd + ledMixIn` (relative error
≤1e-12 in transfer fixtures). K0 only queues due cadence events; topology mutation occurs
after K4.

**M2.1 — mantle and plates.** `SIM.cool()` already owns `Tm(t)`; pass its value into the
kernels rather than reimplementing cooling. Add `mantle.js` for the seeded, integer-harmonic
periodic streamfunction in design §4.1 and the surface extension `ext = d(uMantle-uPlate)/dx`
(in 1/Myr, sampled by periodic centred differences at column centres; `extRef` is applied
by the damage update). Plumes wait for M4. Advect `Tf` with
the design's semi-Lagrangian method into the existing scratch buffer, with periodic-x and
coarse/fine sampling; reserve the symmetric neighbour stencil for symmetric diffusion, not
advection. Add `plates.js` K2 weighted mean-fit using actual `colW`, relax by `dtGeo/tauOmega`,
include ridge push and previous-frame C–C resistance (defer slab pull until M4 ribbons
exist), and clamp to `vMax`. Its K3 classifier uses
`relN = uRight-uLeft` (positive opens, negative closes), `epsHi/epsLo` hysteresis, and the
polarity rules in design §4.2; preserve prior edge state only for the same ordered plate
pair. Validate wrap periodicity to double-precision tolerance, streamfunction divergence,
constant-T preservation, finite/bounded temperatures and speeds, zero-dt no-op, and
prescribed two-plate opening and closing thresholds. Transform shear is out of scope: this
model has no transverse plate
velocity, so a neutral contact must not invent `relT` damage.

**M2.2 — K3 transport and K4 contacts.** Save each column's old width, translate by its
plate velocity, wrap into `[0, P.wrap)`, then stable-sort a permutation by `(wrapped x,
old index)`. Gather the *entire* column record and layer stack through that permutation—not
only `colX`—and remap `depCol`/`venCol` through the inverse permutation. Build gap/overlap
intents from post-move sorted geometry and signed `relN`, including the last-to-first seam
via `GEO.nextColX`. A gap is uncovered when its midpoint is farther than
`rGap*w0` from both columns (`spacing > 2*rGap*w0`) and the boundary is opening, so the
initial `w0` pitch cannot spawn crust and `dtGeo = 0` cannot create a ridge. An overlap is
a different-plate pair below `rContact*w0` in the hysteretic closing state. Resolve intents
in a separate deterministic gather/apply pass, never mutate the list being scanned. Apply
consumes before births so freed slots are available, then write the final topology to a
sorted output buffer, recompute `plN`/anchors/widths, and rebuild reciprocal column,
deposit and vent indices. A rift share transfers material from the nearest K donors on
both sides only when both donor sets are continental; otherwise oceanic crust is recorded
as a mantle source. Assign each newborn to the left plate of its opening edge (including
the wrapped last-to-first edge); this fixed tie rule
avoids creating a plate for every birth. If `colCap` is full, skip that spawn atomically
and increment a diagnostic; do not thin donors or update the ledger.
For subduction, transfer half of the consumed oceanic sediment to an overriding-margin
prism and account the remaining stack by lithology as an explicit M2 sink; M4 routes new
subduction transfers into ribbon storage instead. C–C collision transfers the complete stack
of the thinner-felsic column to the winner; it creates no subduction prism. Neutral contacts
are mass-neutral.

Because `colW` is recomputed from spacing, pure movement must not create/destroy mass:
perform K4 transfers in volume units (`layer thickness × old width`), then convert the
surviving/recipient volumes to layer thickness only after final widths are known. Rebuild
all column caches after these writes. Apply due split/suture events after K4 and before K5/
K6 surface work; do not mutate a sorted array during iteration. Test no duplicate/lost
records (including stacks, deposits and vent ownership), exact seam continuity, one gap
packet per qualifying opening, collision/subduction polarity, and ledger balance per lith.

**M2.3 — dynamic surface response.** Use the M1 `SURF.elev()` and `SURF.profile()` as the
age-dependent isostatic base; do not reimplement thermal subsidence. In K5, update
age/damage, apply the signed trench source and relaxation to `zDyn`, then use the periodic,
width-weighted symmetric flexure stencil from design §4.4. Collision relief comes from the
isostatic layer sums; do not add a second uncalibrated load-to-flexure term. K6 recomputes
the profile once after all K3–K5 topology and surface fields are final. Erosion/deposition
stay in M3. Check `dtGeo = 0`, trench sign, a prescribed localized `zDyn` load's finite
flexural response, and collision isostasy without changing the lithology mass ledger.

**Done:** `m2-check.js` passes finite/sorted/unique/periodic and per-lithology balance
checks; zero tectonic time is a no-op; mantle velocity repeats after `P.wrap` within
double-precision tolerance;
prescribed plate pairs enter and leave opening/closing hysteresis at the specified
thresholds; a symmetric ridge creates balanced crust (±20% counts over 20 Myr at
50 kyr/frame); long-run consumed width tracks integrated convergence to ±10% after
contact-threshold startup; C–C collision thickens the winner with finite isostatic
relief; and a prescribed localized `zDyn` load yields a finite foreland flexure response.
Same seed and same `dtGeo` are bitwise deterministic. Compare 10/50/100 kyr
prescribed fixtures by integrated created/consumed width and ledger (within ±10%), not
identical event topology. `smoke.js`, `view-check.js` and the raster budget remain green.

### M3 — Sediments (2 d)

M3.1 `surface.js` erosion (knee law), one-hop routing gather, deposition layers with
    `wet/unconformity` flags, turbidite half-pass; placer load riding the sediment.
M3.2 Ledger extension: erosion↔deposition exact; `unconformity` flag when a layer top
    is beveled before burial.

Done: a cone sheds to its adjacent basin and the fill burs in visible beds (tabular,
onlapping the flank at the column scale); total crust+mobile mass constant to 1e-6
through a 50 Myr run; placer shows downslope of its source in the probe.

### M4 — Slabs, plumes, melts (3 d)

M4.1 `slab.js` ribbons: consumed stacks append, polyline descent with dip steepening,
    respacing; cold anomaly fed into fan T.
M4.2 `mantle.js` plumes (heads + conduit markers, LIP growth on arrival);
    `magma.js` wedge melt from ribbon water × fan T, chamber fill, sill/underplate spill.
M4.3 `crust.js` arc enrichment from ribbon recycling (`kRec`), collapse diffusion
    active above `hCollapse`.

Done: a subducting slab is a visible descending stratified tongue with a cold halo; arc
vent supply correlates with convergence rate (measure in `experiments/ledger-check.js`
water/melt ledger); a plume arrives, swells, and leaves an LIP stack on the column;
after ~4 Gyr `|u|` decays below 1 mm/yr (stagnant lid).

### M5 — Toy eruptives and the second clock (3–4 d)

M5.1 `erupt.js` toy box: pile CA (repose slough), freeze, crater/dome styles;
    particles (ballistic pyroclastics, buoyant ash); `magma.js` eruption schedules on
    the eruptive clock (effusive vs explosive from gas + pressure).
M5.2 Write-back: mass → stack layers (`tephra`, `lava`), pile → edifice record;
    `render.js` cone fill clipped at the profile + eruption FX overlay.

Done: at default zoom a stratocone grows to ~17 px wide and ~16 px tall and erupts with
a plume and pyroclastics at the 30 min/frame default (minutes-scale at the slider max);
tephra and lava layers appear in the probe with correct mass (ledger); empty chamber
idles the toy under fast geology, over-full chamber builds sills instead (ledger);
`experiments/erupt-bench.js` ≥ 60 toy steps/s at 16 vents.

### M6 — Deposits, save/load, polish (2–3 d)

M6.1 `ore.js` depth-resolved deposits (design §4.7) + extraction overlay and ranked list;
    deposit symbols in `render.js`.
M6.2 `state.js` JSON save/load round trip (bit-identical resume), `ui.js` buttons and
    seed entry; `sim.js` diag HUD (invariants every 2 Hz).
M6.3 Acceptance pass (below), `archive/0.1.0-worklog.md`, tune defaults from
    `experiments/` numbers.

Done: full acceptance below; save → reload → 1000 frames reproduces the live run
bitwise; the plan's numbers still match `scale-check.js`.

## 3. Acceptance (whole project)

1. Scales and geometry: `node experiments/scale-check.js` and `node experiments/view-check.js`
   print ALL PASS and match the design §1 tables (fan 7155, w0 78.18 km, the isostasy
   calibration points, the stencil symmetry).
2. Rigid transport: 3 columns of one plate migrate 2000 km at 5 cm/yr with exact spacing
   (wrap crossing included); no drift over 10^5 frames at dt 50 kyr.
3. Crust cycle: prescribed convergence reproduces the consumed length (±10%) and the
   prism/arc mass ledger; prescribed divergence spawns rift-share margins thinning
   35 → 15 km over ~3 columns before oceanic crust starts.
4. Sediments: cone-to-basin ledger exact (1e-6); beds accumulate visibly; an
   unconformity cuts older beds in the probe.
5. Slab-to-arc: water released at 50–200 km equals melt delivered ±20%; the arc sits
   1–3 columns behind the trench on the overriding side.
6. Eruptions: one effusive and one explosive run each build a 10–30 px edifice at the
   default window with conserved mass; write-back layers read back through the probe.
7. Deposits: > 60% of `oArc` deposit mass sits under edifice/arc columns; placer downslope
   of orogenic/arc sources; VMS on buried seafloor horizons.
8. Stability: 500 Myr at dt 10 kyr and 100 kyr with finite state, ledger drift < 0.1%
   (excluding recorded sources), < 5% boundary flicker per frame.
9. Performance: body raster ≤ 6 ms (min-of-runs, so a throttled shared CPU cannot fail a
   fast implementation) and full frame ≤ 16 ms at 1280×560 on a desktop
   (`experiments/raster-bench.js`); HUD rebuild at 2 Hz only.
10. Determinism: same seed + sliders ⇒ bitwise-identical 1000-frame `state.hash()`
    (`experiments/smoke.js` runs it twice and compares).
